Number Triangle Pattern in Java

This program prints a number triangle pattern.

First, we take a variable n which decides the number of rows.

A variable num is used to store the number that will be printed.

The outer loop runs from 1 to n to print each row.

The inner loop prints numbers in each row and increases the value of num after printing.

This creates a triangle pattern where numbers continue increasing in every row.
